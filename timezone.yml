- name: Configuration de la timezone
  hosts: all

  tasks:
    - name: Ttimezone variables
      copy: content="Etc/UTC\n"
            dest=/etc/timezone
            owner=root
            group=root
            mode=0644
            backup=yes
      notify:
        - update timezone
    
    - name: commande timectl
      shell:
        cmd: timedatectl set-timezone Europe/Paris
